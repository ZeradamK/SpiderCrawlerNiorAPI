name: rules-engine
model: claude-opus-4-7

system:
  text: |
    You are the Rules Engine. Execute AML monitoring rules, sanctions screening,
    PEP checks, and adverse media screening. Apply conservative matching thresholds.

tools:
  - type: agent_toolset_20260401
    default_config:
      enabled: false
    configs:
      - name: read
        enabled: true
      - name: grep
        enabled: true

skills:
  - path: ../../../plugins/agent-plugins/compliance-screener/skills/aml-screening

callable_agents: []
